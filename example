(do
    (= a 1)
    (if (== a 1)
        (putl "ok")
        (putl "no")
    )
    (#
    (= h 10)
    (for (= i 0) (< i h) (+= i 1)
        (putl
            (* " " (- h i))
            (* " " (- (* i 2) 1))
            (if (< i 1)
                "🭐"
            )
        )
    )
    )
)

